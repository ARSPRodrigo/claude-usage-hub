services:
  server:
    build: .
    image: claude-usage-hub:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - data:/data
    env_file:
      - .env

volumes:
  data:
    driver: local
